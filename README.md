### Simple React HOC Example

The HOC pattern consists of creating reusable React Components that can be plugged around other React Components.

create a new React project:

create-react-app my-app
cd my-app
npm start